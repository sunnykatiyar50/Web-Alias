{
  "name": "web-alias",
  "version": "2.0.3",
  "description": "Web alias manager and URL shortcut launcher. Type a keyword in the address bar to instantly navigate to any saved URL.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}